# Todo
Simple Todo List App

pip install django

Should enter database name in settings.py
like this;

DATABASES = {
       'default': {
           'ENGINE': 'django',
           'NAME': '<Database Name>',
       }
 }

python manage.py runserver
